# ============================================================================
# Phase 2, Step 1: SFT Warm Start (Vertex AI Supervised Tuning)
# ============================================================================
# Install with:  pip install -r requirements.txt
# Or with uv:    uv pip install -r requirements.txt
#
# No GPU stack: training runs on Google's hardware via Vertex AI's hosted SFT.
# Your laptop only submits jobs and polls. Inference uses LiteLLM (already
# pulled in by mini-swe-agent).
# ============================================================================

# Vertex AI SDK (supervised tuning + tuned-endpoint inference)
google-cloud-aiplatform>=1.71.0

# GCS (upload tuning dataset)
google-cloud-storage>=2.18.0

# Newer unified Gen AI SDK (used for token counting)
google-genai>=1.18.0

# SWE-bench Lite problem statement loading
datasets>=3.0.0

# Config
pyyaml>=6.0

# Optional: for nicer CLI output
tqdm>=4.66.0
