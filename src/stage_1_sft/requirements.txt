# ============================================================================
# Phase 2, Step 1: SFT Warm Start -- Pinned Dependencies
# ============================================================================
# Install with:  pip install -r requirements.txt
# Or with uv:    uv pip install -r requirements.txt
# ============================================================================

# Core ML framework
torch>=2.1.0

# HuggingFace ecosystem
transformers>=4.45.0
datasets>=3.0.0
accelerate>=1.0.0
tokenizers>=0.20.0

# Parameter-efficient fine-tuning
peft>=0.13.0

# Reinforcement Learning from Human Feedback trainer
trl>=0.12.0

# 4-bit quantization for memory-efficient training
bitsandbytes>=0.44.0

# Configuration
pyyaml>=6.0

# Logging and monitoring
tensorboard>=2.15.0

# Tokenizer dependencies (needed by some models)
sentencepiece>=0.2.0
protobuf>=4.25.0